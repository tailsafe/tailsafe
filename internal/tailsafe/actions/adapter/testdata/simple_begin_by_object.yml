type: object
properties:
  number:
    type: number
    value: "{{ object.number }}"
  string:
    type: string
    value: "{{ object.string }}"
  boolean:
    type: boolean
    value: "{{ object.boolean }}"
  datetime:
    type: datetime
    value: "{{ object.datetime }}"
  sub_object:
    type: object
    properties:
      number:
        type: number
        value: "{{ object.number }}"
      string:
        type: string
        value: "{{ object.string }}"
      boolean:
        type: boolean
        value: "{{ object.boolean }}"
      datetime:
        type: datetime
        value: "{{ object.datetime }}"